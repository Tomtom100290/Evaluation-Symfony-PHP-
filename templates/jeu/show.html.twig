{% extends 'base.html.twig' %}

{% block title %}Jeu{% endblock %}

{% block body %}
    <h1>Jeu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jeu.id }}</td>
            </tr>
            <tr>
                <th>NomJeu</th>
                <td>{{ jeu.NomJeu }}</td>
            </tr>
            <tr>
                <th>NbParticipants</th>
                <td>{{ jeu.NbParticipants }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_jeu_index') }}">back to list</a>

    <a href="{{ path('app_jeu_edit', {'id': jeu.id}) }}">edit</a>

    {{ include('jeu/_delete_form.html.twig') }}
{% endblock %}
